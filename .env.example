# MiniDesk configuration
# Copy this file to .env and adjust ports if the defaults clash with
# anything on your network. All values are optional.

# Port for the web UI (open http://localhost:<APP_PORT>)
APP_PORT=6969

# UDP port used for device discovery broadcasts
BROADCAST_PORT=7979

# TCP port the remote command server listens on
COMMAND_PORT=9000